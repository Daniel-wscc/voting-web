version: '3.8'

services:
  voting-app:
    container_name: decidely-voting-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - voting_data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DATABASE_PATH=/app/data/voting.db

volumes:
  voting_data:
    driver: local
